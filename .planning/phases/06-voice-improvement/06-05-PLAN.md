---
phase: 06-voice-improvement
plan: "05"
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
  - "06-04"
files_modified: []
autonomous: false
requirements:
  - VOIC-MUTE
  - VOIC-DEAFEN
  - VOIC-SPEAKING
  - VOIC-VAD
  - VOIC-DEVICES
  - VOIC-SORT

must_haves:
  truths:
    - "Mute and Deafen buttons visible and functional in VoiceBar"
    - "Speaking indicators appear as green rings in sidebar"
    - "Voice Activity mode sends audio automatically when threshold exceeded"
    - "Device picker populates with real device labels after mic permission"
    - "Preferences persist across page reload"
    - "Members sidebar shows voice participants at top of Online section"
---

<objective>
Human verification of all Phase 6 voice improvement features across all six requirement areas.

Purpose: All features were built in plans 01-04. This checkpoint confirms the complete voice experience works correctly end-to-end in the running application.
Output: Verified working voice improvement features.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify all Phase 6 voice improvements end-to-end</name>
  <action>Pause execution and wait for human to verify the running application.</action>
  <what-built>
    Phase 6 Voice Improvement — all features:
    - VOIC-MUTE/DEAFEN: Mute and Deafen buttons in VoiceBar, muted/deafened icons in sidebar participant list
    - VOIC-SPEAKING: Green ring on active speaker avatars in left sidebar voice channel participant list
    - VOIC-SORT: Members sidebar sorts voice participants to top of Online section
    - VOIC-VAD: Voice Activity Detection mode as alternative to PTT, with sensitivity slider
    - VOIC-DEVICES: Mic and speaker device pickers in settings, Test Mic with live level visualizer
  </what-built>
  <how-to-verify>
    Start the server: `mix phx.server` (with docker compose up for Postgres)

    **VOIC-MUTE/DEAFEN (plan 01):**
    1. Open two browser tabs, log in as two different users on each
    2. Both join the same voice channel
    3. On Tab 1: click Mute button in VoiceBar — button should turn red, mic icon changes
    4. On Tab 2: check left sidebar participant list — should see mic-off icon next to Tab 1 user
    5. On Tab 1: click Deafen — button turns red, all remote audio is silenced, Mute also turns red
    6. On Tab 2: should see headphone-off icon next to Tab 1 user in sidebar
    7. On Tab 1: click Unmute — only mic unmutes; Deafen still active (user still can't hear)
    8. On Tab 1: click Undeafen — remote audio resumes
    9. On Tab 1: while Muted, try PTT (spacebar) — should NOT transmit audio

    **VOIC-SPEAKING (plan 02):**
    1. With two users in voice, Tab 1 user presses spacebar (PTT active)
    2. On Tab 2: Tab 1 user's avatar in left sidebar participant list should show green ring
    3. Tab 1 releases spacebar — green ring disappears
    4. Tab 1 user disconnects from voice — no ghost ring remains

    **VOIC-SORT (plan 02):**
    1. With one user in voice and others online but not in voice
    2. Check the right-side Members sidebar
    3. Voice participant should appear FIRST in the Online section

    **VOIC-DEVICES + VOIC-VAD (plans 03, 04):**
    1. Navigate to /users/settings
    2. Scroll to Voice Settings section
    3. Click "Test Mic" — browser asks for mic permission — approve
    4. Device dropdowns should populate with real device names (not empty strings)
    5. Speak into mic — audio level bar should animate (green bar moves)
    6. Click "Stop Test" — bar disappears
    7. Switch to "Voice Activity" mode, adjust sensitivity slider
    8. Click "Save Voice Settings" — flash shows "Voice preferences saved."
    9. Refresh page — Voice Activity radio still selected, slider at saved value
    10. Join a voice channel — VoiceBar should show "Voice Activity" label instead of PTT button
    11. Speak into mic — audio should transmit automatically when loud enough
    12. Stop speaking — audio transmission stops
    13. Switch back to "Push to Talk" — save — PTT button returns in VoiceBar
  </how-to-verify>
  <verify>Human visual and functional verification of the running application</verify>
  <done>All six VOIC requirements verified working in the running application</done>
  <resume-signal>Type "approved" if all features work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human verified all six VOIC requirements pass end-to-end in the running application.
</verification>

<success_criteria>
- Mute and Deafen controls work bidirectionally with correct state propagation
- Green speaking ring appears and disappears correctly in sidebar
- Members sidebar sorts voice participants first
- Voice Activity Detection transmits audio based on threshold
- Device picker shows labeled devices and persists selection
- All preferences survive page reload
</success_criteria>

<output>
After completion, create `.planning/phases/06-voice-improvement/06-05-SUMMARY.md`
</output>
